# 1.0.9 variance_threshold 도출 로직에 log-Otsu 단계 도입 (장꼬리 분포 대응)

---

## (사용할 skills: micro-fix)

## 개요

`fig4_bg_variance_hist.png` 시각화 결과, 배경 패치의 variance 분포가 단봉(unimodal) + 우편향 장꼬리 형태임을 확인했다. 현재 3단계 사다리(`valley → otsu_1d → percentile`)에서 valley는 단봉이라 실패하고, otsu_1d는 장꼬리에 끌려 **1389.4016**을 반환하여 전체 패치의 95%+가 smooth로 분류되는 문제가 발생한다. `log(variance + 1)` 변환 후 Otsu를 적용하면 우편향이 완화되어 의미 있는 분리점을 도출할 수 있다. 새 사다리: `valley → log_otsu → otsu_1d → percentile(p50)`.

---

## 영향도 분석

### 이 기능이 변경하는 상태
- `threshold_recommendations.json`의 `variance_threshold.recommended` 값 변경 (1389.4016 → 100~300 예상)
- `threshold_recommendations.json`의 `variance_threshold.method` 변경 (`otsu_1d` → `log_otsu`)
- `recommended_config.yaml`의 `background_analyzer_thresholds.variance_threshold` 값 변경

### 그 상태를 전제로 동작하는 기존 로직
- `src/analysis/background_analyzer.py` — `variance_threshold`를 smooth 분류 경계로 사용. 값이 낮아지면 더 많은 패치가 non-smooth로 분류됨 → 배경 유형 분류 정확도 향상 (의도된 변화)
- Stage A `extract_rois.py` — `--config`로 `recommended_config.yaml`을 읽어 `variance_threshold` 적용. 재실행 시 ROI 배경 분류 결과가 바뀔 수 있음

---

## 수정 내용

### 1. `scripts/analyze_dataset.py` — `_log_otsu()` 신규 함수 추가

`_otsu_1d()` 바로 아래에 추가. 내부에서 `_otsu_1d()`를 재사용하는 컴포지션 구조:

```python
def _log_otsu(values: np.ndarray) -> float | None:
    """log1p 변환 후 Otsu 적용, expm1으로 역변환. 장꼬리 분포 대응."""
    pos = values[values >= 0]
    if len(pos) < 50:
        return None
    log_vals = np.log1p(pos)
    t_log = _otsu_1d(log_vals)
    if t_log is None:
        return None
    t = float(np.expm1(t_log))
    if not np.isfinite(t) or t <= 0:
        return None
    if not (float(pos.min()) < t < float(pos.max())):
        return None
    return t
```

### 2. `scripts/analyze_dataset.py` — `derive_threshold()` 시그니처 확장

`use_log_otsu=False` 파라미터 추가, valley와 otsu_1d 사이에 log-Otsu 분기 삽입:

```python
def derive_threshold(
    values: np.ndarray,
    hard_default: float,
    pct_fallback: int = 75,
    use_log_otsu: bool = False,
) -> tuple[float, str]:
    """Ladder: valley → [log_otsu] → Otsu_1D → percentile → hardcoded."""
    finite = values[np.isfinite(values)] if len(values) else values
    if len(finite) < 50:
        return hard_default, "hardcoded(insufficient_n)"

    v = _valley_threshold(finite)
    if v is not None and float(finite.min()) < v < float(finite.max()):
        return round(v, 4), "valley"

    if use_log_otsu:
        lo = _log_otsu(finite)
        if lo is not None:
            return round(lo, 4), "log_otsu"

    o = _otsu_1d(finite)
    if o is not None and float(finite.min()) < o < float(finite.max()):
        return round(float(o), 4), "otsu_1d"

    p = float(np.percentile(finite, pct_fallback))
    return round(p, 4), f"p{pct_fallback}"
```

### 3. `scripts/analyze_dataset.py` — `threshold_specs` 튜플 6필드화

튜플에 `use_log_otsu` 필드 추가. `variance_threshold`만 `True`:

```python
threshold_specs = [
    # (name,                  array,                  default,  pct,  derivable, use_log_otsu)
    ("HIGH_LINEARITY",        arr["linearity"],        0.85,    85,   True,  False),
    ("HIGH_ASPECT_RATIO",     arr["aspect_ratio"],     5.0,     90,   True,  False),
    ...
    ("variance_threshold",    bar["variance"],         100.0,   50,   True,  True),   # ← True
    ("edge_threshold",        bar["v_ratio"],          0.3,     75,   True,  False),
    ...
]
```

도출 루프도 6필드 unpack으로 수정:
```python
for name, data, default, pct, derivable, use_log_otsu in threshold_specs:
    if derivable:
        val, method = derive_threshold(data, default, pct, use_log_otsu=use_log_otsu)
    ...
```

---

## 수정 대상 파일

- `scripts/analyze_dataset.py`

---

## 테스트

```python
# 빠른 검증 (200장 샘플)
!python $SCRIPTS/analyze_dataset.py \
    --image_dir $TRAIN_IMAGES \
    --train_csv $TRAIN_CSV \
    --output_dir $ANALYSIS_DIR \
    --max_images 200 \
    --num_workers 2
```

```python
# variance_threshold method 및 값 확인
import json
with open(f"{os.environ['ANALYSIS_DIR']}/threshold_recommendations.json") as f:
    recs = json.load(f)
print(recs["variance_threshold"])
# 기대: {"recommended": <100~300>, "method": "log_otsu", ...}
```

```python
# 다른 12개 항목 회귀 확인 — method가 기존과 동일해야 함
for k in ["HIGH_LINEARITY", "HIGH_ASPECT_RATIO", "edge_threshold", "high_freq_ratio"]:
    print(k, recs[k]["method"])
```

```python
# fig4 빨간선 위치 육안 확인
from IPython.display import Image
Image(f"{os.environ['ANALYSIS_DIR']}/figures/fig4_bg_variance_hist.png")
# 기대: 빨간선이 분포 본체 우측 경계(100~300) 근처
```

---

## TODO

- [ ] log-Otsu가 단봉 분포에서도 중앙값 근처 임의 분리점을 반환할 수 있음 — 실행 결과 보고 합리성 판단
- [ ] `total_strength`, `high_freq_ratio` 등 다른 long-tail 의심 항목에도 log-Otsu 확대 적용 여부 — fig5, fig6 시각화 재확인 후 별건 결정
- [ ] `threshold_specs` 외에 튜플을 unpack하는 다른 코드 경로 존재 여부 구현 시 grep 확인
